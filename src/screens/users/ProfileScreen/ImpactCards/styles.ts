import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  cardsContainer: {
    flexDirection: "row",
    flexWrap: "wrap",
    paddingLeft: 16,
    paddingRight: 8,
    paddingTop: 8,
  },
});

export default styles;
