import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    flexWrap: "wrap",
    paddingLeft: 16,
    paddingRight: 8,
    paddingTop: 8,
    marginTop: 8,
  },
});

export default styles;
