import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    borderRadius: 8,
  },
  border: {
    borderColor: "#EDEDED",
    borderWidth: 1,
  },
});

export default styles;
