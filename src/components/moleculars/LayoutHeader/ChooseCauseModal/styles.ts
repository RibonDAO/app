import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  imageContainer: {
    display: "flex",
    width: 296,
    height: 136,
    resizeMode: "cover",
    borderRadius: 4,
    marginBottom: 16
  },
  imageBackground: {
    width: '100%',
    height: '100%',
    borderRadius: 4,
  }
});

export default styles;
